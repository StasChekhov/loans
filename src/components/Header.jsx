import s from "./components.module.css";

const Header = () => {
 return (
  <header className={s.header}>
   <h2 className={s.headerArticle}>Current Loans</h2>
  </header>
 );
};

export default Header;
